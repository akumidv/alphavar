"""alphavar.io — domain-neutral data infrastructure (R0).

I/O layer shared across domains: ``provider`` (data access abstraction),
``exchange`` (HTTP venue clients), ``messanger`` (notifications). An exchange
returns options, futures and — in future — equities, so it is infrastructure,
not a domain.
"""
