"""alphavar.options.lib — pure computational logic for the options/futures domain (R0).

DataFrame in → DataFrame out: no I/O, no network, no provider/exchange or facade
imports, no global mutable state. Organized by function (``analytic``, ``chain``,
``chart``, ``enrichment``, ``normalization``). This is the strategic Polars-port
target (R8) and is independently extractable.
"""
