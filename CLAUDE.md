# CLAUDE.md

This project uses **[AGENTS.md](AGENTS.md)** as the single source of guidance for AI
coding agents (including Claude Code).

Claude Code auto-loads `CLAUDE.md` but **not** `AGENTS.md`, so AGENTS.md is imported below.
This keeps the canonical rules — including the always-on prime directives (**D2** owner
verifies math/DataFrame/architecture, **D5** owner owns commits) and "read `_forge/memory/`
at session start" — present in context from the start, instead of one hop behind a pointer.

@AGENTS.md
